@namespace ProjectBullet.UI.HudElements
@using Sandbox
@inherits Sandbox.UI.Panel;

<style>
    DeathFeed {
        position: absolute;

        top: 20px;
        right: 20px;
        
        flex-direction: column;
    }
</style>

<root>
</root>

@code {

    public void AddEntry(Entity victim, Entity attacker)
    {
        AddChild(new DeathFeedEntry(victim, attacker));
    }
}