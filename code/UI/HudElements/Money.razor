@namespace ProjectBullet.UI.HudElements
@using ProjectBullet.UI.Helpers
@using Sandbox.UI
@inherits Sandbox.UI.Panel;
@attribute [StyleSheet("/UI/HudElements/Money.scss")]

<root>
    <Corner class="@Util.WorkshopOpenClass">
        🪙@Util.LocalPersistent.Money
    </Corner>
</root>

@code {

    protected override int BuildHash()
    {
        return Util.LocalPersistent.Money;
    }

}